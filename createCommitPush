# Create a single folder executable (run existing script)
./createSingleFileExecutable

# git commit chamges with a default name (Windows test....")
git commit -am "Commit changes to remote"

# push to remote repo
git push
